# 🔴 GITHUB COPILOT - REGRAS DE EXECUÇÃO OBRIGATÓRIA

## ⚠️ REGRA MÁXIMA: NUNCA USE `cat <<EOF`

**PROIBIDO ABSOLUTAMENTE:**
```bash
# ❌ NUNCA FAZER ISTO
cat <<EOF
conteúdo
EOF

# ❌ NUNCA ISTO TAMBÉM
echo "conteúdo" > arquivo

# ❌ NUNCA USAR HEREDOC
cat > arquivo <<'EOL'
conteúdo
EOL

# ❌ NUNCA USAR PIPE COM ECHO
echo "conteúdo" | tee arquivo
```

---

## ✅ PADRÃO OBRIGATÓRIO: 3 PASSOS

### Passo 1: CRIAR arquivo com `create_file` tool
```bash
create_file /caminho/arquivo.txt "conteúdo aqui"
```

### Passo 2: EXIBIR com `cat` command
```bash
cat /caminho/arquivo.txt
```

### Passo 3: DELETAR (se temporário)
```bash
rm /caminho/arquivo.txt
```

---

## 🚫 CASOS ONDE JAMAIS USAR `cat <<EOF`

- ✅ Criar scripts shell → use `create_file`
- ✅ Criar arquivos de configuração → use `create_file`
- ✅ Criar arquivos YAML → use `create_file`
- ✅ Criar arquivos JSON → use `create_file`
- ✅ Criar documentação → use `create_file`
- ✅ Exibir resumos → use `create_file` → `cat` → `rm`
- ✅ Exibir resultados → use `create_file` → `cat` → `rm`

---

## 📋 CHECKLIST ANTES DE QUALQUER OPERAÇÃO

Antes de executar QUALQUER comando que envolva criação de arquivo:

- [ ] Vou usar `create_file` tool com conteúdo completo?
- [ ] Vou exibir com `cat /caminho/arquivo` (NOT `cat <<EOF`)?
- [ ] Vou deletar arquivo temporário com `rm`?
- [ ] NÃO estou usando echo, printf, heredoc, ou pipe?
- [ ] O arquivo foi criado com `create_file` tool (não terminal)?

---

## 🎯 RAZÃO DESTA REGRA

1. **GitHub Copilot TEM permissão total** para manipular arquivos
2. **Não há razão técnica** para usar `cat <<EOF`
3. **Melhor rastreabilidade** com `create_file` tool
4. **Compatibilidade CI/CD** garantida
5. **Auditoria e git tracking** funcionam melhor
6. **Sem problemas com caracteres especiais** ou escaping

---

## 📝 EXEMPLOS REAIS

### ❌ ERRADO (NUNCA FAZER)
```bash
# Scenario: Criar arquivo de teste
cat > /tmp/teste.txt <<'EOF'
linha 1
linha 2
EOF
cat /tmp/teste.txt
```

### ✅ CORRETO (SEMPRE FAZER ASSIM)
```bash
# Scenario: Criar arquivo de teste
# Step 1: Create with create_file tool
create_file /tmp/teste.txt "linha 1
linha 2"

# Step 2: Display with cat command
cat /tmp/teste.txt

# Step 3: Delete if temporary
rm /tmp/teste.txt
```

---

## 🔒 APLICAÇÃO UNIVERSAL

Esta regra se aplica a:
- Scripts shell
- Arquivos de configuração
- Resumos de execução
- Apresentações de resultados
- Demais apresentações
- **TODAS as situações sem exceção**

---

## 📌 LEMBRETE CRÍTICO

> **"GitHub Copilot tem PERMISSÃO TOTAL para manipulação de arquivos. Use `create_file` tool SEMPRE."**

**NÃO HÁ EXCEÇÕES. NÃO HÁ CASOS ESPECIAIS. NENHUMA SITUAÇÃO JUSTIFICA `cat <<EOF`.**
